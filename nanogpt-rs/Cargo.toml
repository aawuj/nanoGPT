[package]
name = "nanogpt-rs"
version = "0.1.0"
edition = "2021"
description = "A pure-Rust CPU reimplementation of nanoGPT (train + sample)"

# Zero third-party dependencies: RNG, thread pool, checkpointing and all
# numerics are implemented in-crate.

[profile.release]
opt-level = 3
lto = "thin"
